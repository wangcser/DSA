# 

# 招银网络 CPP 面经

### 程序从源代码到可执行文件的生成过程？

- 预处理：处理代码文件头部声明，生成预编译文件 .i
- 编译：源代码经过词法分析，语法分析，语义分析，代码生成和优化得到对应的汇编代码 .s
- 汇编：将汇编代码转换为机器指令，构成分离的目标文件 .o
- 链接：将目标文件链接为可执行文件 .out / .exe

### const 关键字：

在声明中使用，表示修饰的对象只读

- 修饰变量：只读
- 修饰引用：不可通过引用修改
- 修饰指针：不可通过指针修改，或者指针所指的对象不可修改，或者二者兼有
- 修饰函数参数：参数是只读的
- 修饰类的成员函数：常函数，不可修改成员变量
- 修饰成员函数的返回值：使得返回值不为左值

### const 和 define 的区别？

- 编译器处理方式不同：预处理阶段展开，运行阶段使用
- 类型不同：define 的宏没有类型，const 常量有类型，编译阶段会做类型检查
- 存储方式：宏不会分配内存，常量会被分配内存
- const 更节省内存

### explicit 关键字的作用？

抑制构造函数执行隐式转换

explicit关键字只能用于类内部的构造函数声明上，禁止编译器执行非预期（往往也不被期望）的类型转换

### malloc 和 new 的区别？

- new 关键字执行了两个过程，首先为对象申请并分配内存空间，然后调用类的构造函数构造对象

### 内存泄漏的原因，如何避免内存泄漏？

- 对于动态申请的对象使用过后却忘记释放对象的内存，造成这块内存没有归还操作系统也不能被程序使用
- 人工检查

### 函数覆盖和函数重载的区别？

- 重载：不同函数使用相同的函数名，但是具有不同的：参数个数，参数类型，参数顺序
- 覆盖：派生类中对基类虚函数的重新实现

### 说一说虚函数和抽象类，还有多态？

虚函数：

- 

纯虚函数：

- 

多态：

- 静态多态：重载
- 动态多态：覆盖

### 静态成员变量和普通静态变量的区别？

static 用于控制变量的存储方式和可见性

- 静态成员变量，使用类名直接引用，生命期从声明到程序结束，作用域为局部作用域，同一类内可见
- 普通静态变量，作用域是声明该变量所在的文件

### 接口类和抽象类的区别？

CPP 中没有接口类的概念， CPP 通过抽象类实现接口

- 抽象类指的是类中含有纯虚函数的类

### 造成野指针的原因是什么？

- 释放了指针指向对象的内存却没有清空指针保存的地址

### 为什么要内存对齐，有什么好处？

- 方便 CPU 使用

### overwrite 和 override？



### 拷贝构造函数和赋值构造函数的区别，应用场景？

- 拷贝构造函数：用同一类的对象构造新的对象，是无中生有
- 赋值构造函数：用同一类的对象修改已有的对象，是已有对象的赋值

### 深浅拷贝的区别，深拷贝的实现方式？

- 浅拷贝：只拷贝对象的指针
- 深拷贝：拷贝对象

实现方式：利用对象的指针构造一个新的对象

### 什么是引用，C++ 为什么要引入引用？

- 支持操作符重载
- 指针是为了兼容 C

### 指针和引用的区别是什么？

- 指针是对象，保存另一对象的地址
- 引用是对象的别名

### 使用指针存在什么问题，避免的方法是什么？

- 未初始化
- 初始化未构造
- 访问越界
- 析构后未清空

### 删除一个对象数组的方法？

delete[] p

### 析构函数可以直接调用吗？构造函数？

- 显式调用析构函数，相当于调用普通的成员函数，执行函数内的语句即可
- 隐式调用才会释放内存

### 动态绑定的实现方式，除了用指针还有其他方法吗？

动态绑定：

- 指针：对象
- 虚函数：方法

### 介绍一下 static 关键字？

- 控制变量的存储方式和可见性
  - 全局变量：该文件内可见
  - 局部变量：程序结束前存活

### 虚函数的实现方式？虚函数表里保存了什么？

### 浏览器有前进后退功能，如何实现？

双栈：

- 前进：B -> A
- 后退：A -> B

### 数据库的隔离性有哪些？

### 介绍一下快速排序？

### 数组和链表的区别，各自适用于什么场景？

### 数据库索引适合用于什么场景下，不适合用于什么场景下？

数据库索引是一种排序的数据结构，通过 B 树实现

> https://blog.csdn.net/Mary881225/article/details/70174095

### 进程间通信有哪些？

- 管道
- IPC
  - 消息队列
  - 信号量
  - 共享内存
  - 信号
- socket

### 线程同步的方法？

- 互斥锁 mutex
- 条件变量
- 信号量

### TCP 和 UDP 的区别？

### 多线程中最重要的是什么？

- 线程间同步，共享资源互斥

### 死锁了解吗，出现死锁的原因是什么，如何解决死锁？

- 互斥等待：已分配资源不可共享
- 请求保持：请求其他资源不需要释放已有的资源
- 不可抢占：不能抢占别人的资源
- 环路等待：发生死锁时，必然产生一个进程-资源的环路

方法：

- 人工分配
- 剥夺资源
- 有序分配

### HTTP 请求？

### HTTP 是有状态的吗？

### HTTP 缓存知道吗？

缓存是一种保存资源副本并在下次请求时直接使用该副本的技术

### POST 和 GET 方法知道吗？

- GET，向服务器获取数据
- POST，向服务器发送数据（表单）